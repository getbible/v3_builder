{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "checksum-index.schema.json",
  "title": "Checksum index",
  "description": "The SHA-1 of every document at one level of the tree, keyed as that level is addressed: by abbreviation at the root, by book number in a translation, by chapter number in a book.",
  "type": "object",
  "additionalProperties": {
    "type": "string",
    "pattern": "^[0-9a-f]{40}$",
    "description": "SHA-1 of the document's bytes."
  }
}
