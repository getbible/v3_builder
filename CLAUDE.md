# Working in this repository

Read `AGENTS.md` first; its invariants apply to every change.

Commits, tags, and pull requests are authored in the maintainer's name, the
owner of the GitHub connector: `eWɘyn <5607939+Llewellynvdm@users.noreply.github.com>`.
Never add a `Co-Authored-By` trailer, a session link, an assistant name, or any
other tool attribution. If a commit on a branch you created carries another
author, rewrite it with the right author and force-push that branch.
